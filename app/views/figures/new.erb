<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create a New Figure</title>
  </head>
  <body>

    <h2>Create a New Figure</h2>

    <form action="/figures" method="post">

      <label for="figure_name"><h3>Figure Name:</h3></label>
      <input type="text" name="figure[name]" id="figure_name">

      <h3>Choose an existing title(s) (check all that apply):</h3>
      <% Title.all.each do |title| %>
        <label for="title"><%= title.name %></label>
        <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>">
      <% end %>
      <br>

      <h3>and/or create a new title:</h3>
      <input type="text" name="title[name]" id="new_title">
      <br>


      <h3>Choose an existing landmark(s) (check all that apply):</h3>
      <% Landmark.all.each do |landmark| %>
        <label for="landmark"><%= landmark.name %></label>
        <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%= landmark.id %>">
      <% end %>
      <br>

      <h3>and/or create a new landmark:</h3>
      <input type="text" name="landmark[name]" id="new_landmark">
      <br>

      <input type="submit" value="Create New Figure">

    </form>

  </body>
</html>
